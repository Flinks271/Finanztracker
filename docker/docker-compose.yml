services:
  postgres:
    image: 'postgres:latest'
    ports:
      - "5432:5432"
    env_file:
      - .env
    environment:
      TZ: UTC
      PGTZ: UTC
    volumes:
      - ${PWD}/db-data/:/var/lib/postgresql/data/
